{
    "scripts": {
        "type": "tsc --project jsconfig.json || true",
        "check": "npm run type"
    },
    "devDependencies": {
        "typescript": "^6.0",
        "eslint": "^10.2",
        "@eslint/js": "^10.0"
    }
}
